# serve un PROPERTYSET che non richieda un Hpc, ma definisca un dizionario di proprietà di colore per cella e sfondo bianco.

Esempio fig 3.4, intorno a pagina 70


farebbe comodo una funzione PROPERTYSET con tutti i parametri di colore predefiniti, che l'utente possa cambiare